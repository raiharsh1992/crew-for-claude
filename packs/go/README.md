# go pack — FUTURE (spec-only)

Placeholder pack for Go projects. **Not yet active** — no commands or hooks. It
exists so auto-discovery can report "detected go" against a real pack id.

The `pack.json` declares the marker (`go.mod`) and language. To turn this into a
working pack, follow ["Authoring a new pack"](../PACK-SPEC.md).
